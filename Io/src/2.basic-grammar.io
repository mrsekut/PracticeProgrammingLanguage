Hoge := Object clone
Hoge piyo := "kontiwa" // `piyo`というキーに"kontiwa"を入れる
Hoge piyo = "kontiwa" // `:=`の代わりに`=`でも良いが、すでに存在していれば例外が発生
Hoge piyo // 値を取得

Hoge slotNames // スロットの名前一覧を取得
Hoge type // typeを確認